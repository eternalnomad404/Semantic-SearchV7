faiss-cpu>=1.7.4
pandas>=2.0.0
sentence-transformers>=2.2.0
numpy>=1.24.0
scikit-learn>=1.3.0
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Remove heavy optional dependencies
# streamlit>=1.28.0  (not needed for API)
# openpyxl>=3.1.0   (not needed if data is pre-processed)
# groq>=0.4.0       (not actively used)
# fuzzywuzzy>=0.18.0 (not actively used in search)
# python-Levenshtein>=0.20.0 (not actively used)
